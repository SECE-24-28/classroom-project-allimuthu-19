# Javascript
## what is javascript
Javascript is a client-side, dynamic programming language that we use in conjuction with out HTML file
in order to make it more inertactive.

- Client-side
- Dynamically typed - assign a value to the variable without mentioning its data type
- Interpreted - reads code one line at a tim.